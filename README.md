# Super Powers

## Python setup (pyenv)

Run this once and you are done:

```bash
source tools/create_pyenv.sh
```

This script installs pyenv if needed, creates/activates the `super` env, installs packages, and adds the required pyenv init block to your shell rc file.
